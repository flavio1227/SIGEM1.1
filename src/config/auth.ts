/**
 * Authentication Configuration
 * 
 * URL to redirect users when they are not authenticated.
 * This should point to your SIGEM Login micro-app.
 */
export const LOGIN_URL = 'https://flavio1227.github.io/SIGEM-Login/';
